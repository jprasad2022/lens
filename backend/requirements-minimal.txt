# Minimal requirements for running the system
# These work on Windows without build issues

# FastAPI and core dependencies
fastapi==0.109.0
uvicorn[standard]==0.25.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6

# Basic data processing (no heavy ML libraries)
numpy==1.26.3

# Database and Storage
redis==5.0.1

# Monitoring
prometheus-client==0.19.0

# Utilities
httpx==0.26.0
requests==2.31.0
python-dotenv==1.0.0

# For Kafka (optional - can skip if having issues)
# confluent-kafka==2.3.0